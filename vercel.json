{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "installCommand": "npm install && npm install pg pg-hstore @types/jsonwebtoken @types/bcrypt --no-save"
      }
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
