{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "installCommand": "npm install && npm install pg pg-hstore --no-save && npm install @types/jsonwebtoken --no-save"
      }
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
